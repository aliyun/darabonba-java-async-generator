/**
 *
 */

import TeaRequest;
import ClientConfiguration;

init(configuration: ClientConfiguration){}
/**
 * Execute request
 * @return response
 */
function do(style: string, action: string, method: string, pathRegex: string, bodyType: string, bodyIsForm: boolean, reqBodyType: string, requestModel: $Model): $Model;


/**
 * Validate params for RequestModel
 */
function validateRequestModel(model: $Model): void;

function execute(teaRequest: TeaRequest, model: $Model): $Model;